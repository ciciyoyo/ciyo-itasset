<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ciyocloud.system.mapper.SysRoleMapper">
    <resultMap type="SysRoleEntity" id="SysRoleResult">
        <id property="id" column="id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="menuCheckStrictly" column="menu_check_strictly"/>
        <result property="deptCheckStrictly" column="dept_check_strictly"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectRoleVo">
        select distinct r.id,
                        r.role_name,
                        r.role_key,
                        r.role_sort,
                        r.data_scope,
                        r.menu_check_strictly,
                        r.dept_check_strictly,
                        r.status,
                        r.del_flag,
                        r.create_time,
                        r.remark
        from sys_role r
                 left join sys_user_role sur on sur.role_id = r.id
                 left join sys_user u on u.id = sur.user_id
                 left join sys_dept d on u.dept_id = d.id
    </sql>


    <select id="selectRolePage" parameterType="SysRoleEntity" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        ${ew.getCustomSqlSegment}
    </select>

    <select id="selectRoleList" parameterType="SysRoleEntity" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        ${ew.getCustomSqlSegment}
    </select>


    <select id="selectRolePermissionByUserId" parameterType="Long" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE r.del_flag = '0'
        and r.status = '0'
        and u.id = #{userId}
    </select>

    <select id="selectRoleIdListByUserId" parameterType="Long" resultType="Long">
        select r.id
        from sys_role r
                 left join sys_user_role sur on sur.role_id = r.id
                 left join sys_user u on u.id = sur.user_id
        where u.id = #{userId}
    </select>


    <select id="selectRolesByUserName" parameterType="String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE r.del_flag = '0'
        and u.user_name = #{userName}
    </select>

    <select id="checkRoleNameUnique" parameterType="String" resultMap="SysRoleResult">
        select r.id,
               r.role_name,
               r.role_key,
               r.role_sort,
               r.data_scope,
               r.menu_check_strictly,
               r.dept_check_strictly,
               r.status,
               r.del_flag,
               r.create_time,
               r.remark
        from sys_role r
        where r.role_name = #{roleName}
    </select>

    <select id="checkRoleKeyUnique" parameterType="String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_key = #{roleKey}
    </select>

    <select id="selectRoleUserList" resultType="com.ciyocloud.system.entity.SysUserEntity">
        select u.id,
        u.user_name,
        u.dept_id,
        u.email,
        u.phonenumber,
        u.status,
        u.del_flag,
        u.create_by,
        u.create_time,
        u.update_by,
        u.update_time,
        u.remark
        from sys_user u
        left join sys_user_role sur on sur.user_id = u.id
        left join sys_role r on r.id = sur.role_id
        where r.id in
        <foreach collection="roleIdList" item="id" index="index" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </select>
</mapper>
